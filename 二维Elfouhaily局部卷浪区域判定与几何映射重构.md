# 二维 Elfouhaily 海面局部卷浪区域判定与几何映射重构

## 1. 结论

当前结果中的巨大向下分量不是 Elfouhaily 海面造成的，而是卷曲坐标变换的尺度和角度设置共同造成的。现代码不应继续靠缩小某一个参数修补，而应拆成两个独立问题：

1. 在二维海面上寻找“具备发生卷浪条件的连续波峰区域”；
2. 将 `Ideal_Curl_Wave_Echo.m` 的空间变角旋转应用到该区域的参数化网格顶点，而不是只修改单值高度场。

论文第 II-B 节的基本框架是可用的，但应明确区分“候选位置判定”和“真实破碎触发”。单张静态海面只能提供几何候选，不能仅凭高度宣称已经预测了物理破碎。

## 2. 当前巨大向下分量的来源

当前配置为：

```matlab
pivotDepth = 0.915;
curlMultiplier = 1.16;
verticalAngleRatio = 1.59;
```

在所选波峰中心 `u=0`、`crestWeight=1` 处，有

```text
nZ = pivotDepth = 0.915 m
thetaCurl = 1.16 rad
thetaVertical = 1.59 * 1.16 = 1.844 rad
```

当前垂向公式是

```text
rotZ = -nU sin(thetaVertical) + nZ cos(thetaVertical).
```

因此波峰中心的垂向变化近似为

```text
Delta z = 0.915[cos(1.844)-1] = -1.16 m.
```

这与图中接近 `-0.85 m` 的最低点完全同量级。与此同时，水平前伸量约为

```text
Delta u = 1.05 * 0.915 * sin(1.16) = 0.88 m.
```

所以当前形态实际上是围绕深支点进行大角度旋转产生的长力臂，不是局部卷唇。主要问题有：

- `verticalAngleRatio=1.59` 将水平角和垂向角人为解耦，已经偏离 `Ideal_Curl_Wave_Echo.m` 的同角二维旋转；
- `pivotDepth=0.915 m` 相对 `Hs=0.35 m` 过大，支点深度约为 `2.6Hs`；
- `exp(-abs(u)/amplitudeCurl)` 在传播方向没有严格紧支撑，影响带不是有限区域；
- 变换中心位于最高点附近，而不是论文所述的迎浪肩部；
- 只按最高点选址，没有识别连续波峰脊线、局部坡度和曲率；
- 沿波峰方向只乘固定余弦窗，没有跟踪二维随机海面中实际弯曲的脊线。

## 3. 二维海面上的卷浪区域判定

### 3.1 两级判定

建议将判定分成两级。

**第一级：几何候选区域。** 该级可由单张二维 Elfouhaily 海面计算，用于寻找高而尖锐的连续波峰：

```text
eta >= hq,
abs(eta_u) <= sc,
eta_uu <= -kappa_c.
```

其中 `eta_u` 和 `eta_uu` 是沿主传播方向的一阶、二阶导数。`hq` 应使用高度分位数，而不是固定米制阈值；`sc` 和 `kappa_c` 应由当前海况的导数统计量自适应确定。建议初始取值为：

- `hq`：平滑海面高程的 85% 至 95% 分位数；
- `sc`：`eta_u` 的稳健标准差的 0.20 至 0.35 倍；
- `kappa_c`：负曲率幅值的 70% 至 85% 分位数。

在候选点上还应检查前向肩部的最大下坡坡度，而不是在波峰点检查坡度，因为波峰点理论上一阶坡度接近零：

```text
Sf = max(-eta_u),  u in [0, bf].
```

最后对候选点按八邻域连通，去除长度不足、过于靠近边界或不能容纳完整卷曲支撑的片段。得到的是连续波峰段，不是一个孤立最高点。

**第二级：破碎触发。** 如果有时序海面或表面速度，建议使用运动学触发量，例如波峰处水平粒子速度与波峰传播速度之比达到临界区间。若当前只有静态 Elfouhaily 高度场，就只能使用局部波陡、前坡、曲率和高度的联合经验判据，并在论文中称为“breaking-eligible crest”或“prescribed breaking-event placement”，不能称为普适破碎起始判据。

### 3.2 候选区域评分

对通过门限的点，使用归一化评分：

```text
Q = wh * eta_tilde
  + wk * (-eta_uu)_tilde
  + wf * Sf_tilde.
```

如果以后加入定向短波分量，可再加入论文中的短波贡献项。对每个连通波峰段仅保留一个局部极大值，并设置最小事件间距，避免在同一条波峰上重复生成多个重叠卷浪。

### 3.3 跟踪真实二维波峰脊线

以评分最高点为中心建立传播向坐标 `u` 和波峰向坐标 `v`。对每个 `v` 截面，在中心附近搜索满足 `eta_u=0` 且 `eta_uu<0` 的局部极大点，得到真实脊线 `u_c(v)`。随后对 `u_c(v)` 做轻度平滑，但不能把它替换成直线。

卷曲支撑应围绕

```text
ur = u - u_c(v)
```

建立。这样卷浪会沿二维海面的真实波峰弯曲延伸，而不是把一条剖面横向复制成柱状面。

## 4. 从原始 Ideal Curl 算法保留什么

`Ideal_Curl_Wave_Echo.m` 中真正可复用的是这一核心：在局部传播向-垂向平面内，对不同材料点施加空间变化的同角旋转，并把旋转后的水平坐标和垂向坐标同时映射回世界坐标。

不应直接复用的内容包括固定海底、固定 `z_pivot=-1.5 m`、一维浅水波演化、高斯主波和横向等值复制。这些内容只适合原来的理想柱状波示例。

### 4.1 局部尺度必须来自被选波峰

对每个候选事件估计：

- 局部波高 `Hloc`：波峰到前后相邻波谷或局部平均水面的高度；
- 局部波长 `Lloc`：沿 `u` 方向相邻波谷间距，或相邻同相位波峰间距；
- 波峰段长度 `Lc`：候选脊线的连续长度；
- 传播向半宽 `b`：由局部零曲率点、半高宽或相邻波谷共同确定。

旋转支点深度、过渡长度和预变形幅度均按 `Hloc`、`Lloc` 缩放并设置上限，不能继续使用与海况无关的绝对 `0.915 m`。

特别需要修正论文当前的尺度解释：在 `U10=5 m/s`、`Omega=0.84` 时，谱峰波长约为 `22.7 m`。若强制采用 `ry/lambda_p=0.045-0.059`，垂向卷曲尺度将达到约 `1.0-1.34 m`，是 `Hs=0.35 m` 的约 3 至 4 倍。当前巨大负分量正是这种尺度不匹配的直接表现。后续与 Erinin 实验对比时，必须使用定义一致的局部载波波长，或者先生成与实验波陡和波高相匹配的载波事件，不能把全海面谱峰波长直接套到任意局部短波峰上。

### 4.2 核心区和过渡区分离

建立两个紧支撑窗：

```text
Wc = C(ur / [b r(v)]) C(v/a),
Wt = C(ur / [(b+lt) r(v)]) C(v/a).
```

`Wc` 定义真正的卷曲核心，`Wt` 只负责让周围非卷曲海面逐渐抬升、前倾并平滑回到背景。边界处至少应保证位移连续；最好同时检查一阶导数没有尖峰。

### 4.3 先预变形，再在肩部卷曲

先使用较宽的 `Wt` 对原始海面做低幅预变形：

- 后坡较宽、前坡较窄；
- 整个迎浪区域有小幅前倾；
- 波峰有有限抬升，但抬升量只是 `Hloc` 的小比例。

旋转角中心放在波峰前的迎浪肩部：

```text
u_theta(v) = u_c(v) + dc,  dc < 0.
```

具体正负号必须由代码中的传播方向约定统一。这里约定波沿 `+u` 传播，`dc<0` 表示卷曲从到达最高点前的肩部开始。角度场为有限波峰段上的局部高斯：

```text
theta(u,v) = thetaMax
             exp(-(ur-dc)^2 / [2(0.62b)^2])
             C(v/a).
```

### 4.4 使用同一个旋转角

围绕局部支点 `(u0,z0)` 使用原方法的同角旋转：

```text
[uR]   [u0]   [ cos(theta)  sin(theta)] [up-u0]
[zR] = [z0] + [-sin(theta)  cos(theta)] [zp-z0].
```

删除 `verticalAngleRatio`。支点深度 `dp` 应与 `Hloc` 成比例并钳位，例如先从 `0.25Hloc` 至 `0.60Hloc` 的范围测试，而不是直接取 `0.915 m`。最终结果通过过渡窗混合：

```text
uFinal = up + Wt * (uR-up),
zFinal = zp + Wt * (zR-zp).
```

这种做法仍保留原 Ideal Curl 的卷曲机制，但不会把整个旋转半径转化为不受约束的向下深坑。

## 5. 二维海面的正确数据结构

卷曲后自由表面可能在水平投影上多值，因此结果必须保存为参数曲面顶点：

```matlab
X_final(i,j), Y_final(i,j), Z_final(i,j)
```

而不是重新插值成 `Z=f(X,Y)`。三角面片必须沿用卷曲前结构网格的拓扑连接。不能对 `(X_final,Y_final)` 再做 Delaunay 三角剖分，否则上、下卷唇会被错误跨接。

传播方向映射的雅可比

```text
Ju = d(uFinal)/du
```

用于判断是否真的发生几何翻卷：`Ju<0` 表示局部水平映射反向。高度变化本身不能证明卷曲。

## 6. 新算法的建议执行顺序

```text
输入 Elfouhaily 高度场 Z0
  -> 多尺度平滑，只用于检测
  -> 计算 eta_u、eta_uu 和前向肩部坡度
  -> 高度/近零坡度/负曲率联合候选掩膜
  -> 连通波峰段与事件间距筛选
  -> 跟踪每段真实脊线 uc(v)
  -> 估计 Hloc、Lloc、b、Lc
  -> 建立 Wc 和 Wt
  -> 非旋转预变形，形成前陡后缓和低幅前倾
  -> 在迎浪肩部施加同角局部旋转
  -> 同时更新 X、Y、Z
  -> 保留原结构网格拓扑
  -> 用 Ju<0 检查翻卷
  -> 输出候选掩膜、核心掩膜、过渡掩膜和参数曲面
```

## 7. 必须通过的几何验收

下一版代码至少应自动检查：

1. `Ju<0` 的点数大于零，证明存在真实水平翻卷；
2. 最大向下位移受 `Hloc` 约束，默认不超过约 `0.5Hloc`，除非明确模拟成熟入水卷舌；
3. 最大水平前伸量与 `Hloc`、`Lloc` 同阶，不由固定深支点决定；
4. `Wt=0` 外的三维位移严格为零；
5. 支撑边界处位移和坡度连续，没有环形断崖；
6. 卷浪中心位于迎浪肩部，不与原最高点强制重合；
7. 沿波峰方向的中心线随原海面变化，不退化为等截面柱面；
8. 卷曲前后使用完全相同的顶点编号与面片连接；
9. 中心剖面同时绘制 `u` 和 `z` 坐标，不能只画 `z(x)`；
10. 单独绘制候选区、核心区、过渡区和 `Ju<0` 区，以便判断“选址错误”还是“变换错误”。

## 8. 对论文表述的修正建议

论文第 II-B 节式 (12)-(23) 的结构可以保留，但实验部分不应再把当前参数域称为已经验证的“正确卷浪域”。在重新实现前，应作以下调整：

- 明确式 (12)-(13) 是事件放置规则，不是普适破碎起始判据；
- 将固定谱峰波长归一化改为与实验定义一致的局部载波尺度，或解释尺度匹配过程；
- 删除水平角和垂向角的经验解耦；
- 报告 `max downward displacement/Hloc`，避免只报告卷曲尺度而掩盖深坑；
- 在图中展示参数网格和 `Ju<0` 区，证明它是多值卷曲面而不是负向凹坑；
- 将 G0/G1 设置为同一海面、同一候选区域、同一网格连接，仅是否施加局部变换不同。

## 9. 下一步代码改造边界

下一版仅修改 `Curl` 模块：

- 重写候选区检测为高度、坡度、曲率和连续脊线联合判定；
- 用实际脊线 `u_c(v)` 建立有限二维支撑；
- 删除 `verticalAngleRatio` 和固定大支点；
- 加入局部尺度估计、预变形、肩部同角旋转和过渡混合；
- 输出诊断掩膜、中心参数剖面和原拓扑三角网格。

不引入非线性海面、不引入雷达回波，也不把原一维浅水演化过程移植进来。这样可以先把“二维 Elfouhaily 背景上的局部卷浪几何”单独做正确。
